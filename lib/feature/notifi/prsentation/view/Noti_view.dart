import 'package:flutter/material.dart';



class NotiView extends StatelessWidget {
  const NotiView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("NotiView"),),
    );
  }
}
